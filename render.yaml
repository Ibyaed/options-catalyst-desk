services:
  - type: worker
    name: options-catalyst-desk
    runtime: python
    region: oregon
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python run_scheduler.py